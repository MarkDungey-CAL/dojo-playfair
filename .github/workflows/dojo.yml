name: "dojo"

on:
  workflow_dispatch:
    inputs:
      string_to_decrypt:
        type: string
        required: true
        description:
  workflow_call:
    inputs:
      string_to_decrypt:
        type: string
        required: true
        description:
jobs:
  dojo:
    runs-on: windows-latest
    steps:
      - name: Build Project
        shell: pwsh
        run: |
            dotnet build ./ConsoleApp1yfair/ConsoleApp1/ConsoleApp1.csproj --output ./dojo-playfair.exe
      # - name: Build Project
      #   shell: pwsh
      #   run: |
      #     dotnet run ./dojo-playfair.exe --property VARIABLE:${{inputs.string_to_decrypt}}
          
#"dojo-playfair"

